# Automatically activate UV virtual environment when entering this directory
# Requires: direnv (brew install direnv)
# Setup: Add `eval "$(direnv hook zsh)"` to your ~/.zshrc (or bash equivalent)

# Use UV's virtual environment
source_up_if_exists

# Activate the .venv
source .venv/bin/activate

# Development: Use local dev_projects directory
export ADAM_MCP_DEFAULT_DIR="./dev_projects"

# Optional: Load .env file if it exists
dotenv_if_exists
